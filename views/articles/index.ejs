<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
    integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Blog</title>
</head>

<body>
  <div class="container">
    <h1 class="mb-4">Blog Web Study</h1>
    <a href="/articles/new" class="btn btn-success">New Article</a>
    <br>
    <br>
    <!-- 검색 창 -->
    <div class="container input-group mb-1">

      
      
      <!--input 태그 벨류 가져오기-->
      <!--검색 누를시 어디로? 페이지 이동-->
      <form action="/articles/search">
        <input type = "text" required name = "keyword" placeholder="검색어를 입력하세요" class="form-control" id="search-input">
    
      <button type="submit" class="input-group-append btn btn-light" id="search" >검색</button>
      </form>
     <!--<button type= "submit" class="input-group-append btn btn-light" id="seach">검색</button> -->
    </div>
    <% articles.forEach(article=> { %>
      <div class="card mt-4">
        <div class="card-body">
          <h4 class="card-title">
            <%= article.title %>
          </h4>
          <div class="card-subtitle text-muted mb-2">
            <%= article.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text mb-2">
            <%= article.description %>
          </div>
          <a href="articles/<%= article.slug %>" class="btn btn-primary">Read More</a>
          <a href="articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
          <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
      <% }) %>
  </div>

 

  
 
 
</body>

</html>